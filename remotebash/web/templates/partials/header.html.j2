<header class="border-b border-border">
  <div class="max-w-6xl mx-auto px-6 py-5 flex items-center gap-3">
    <span class="text-2xl">&#9881;</span>
    <h1 class="text-xl font-semibold tracking-tight">RemoteBash</h1>
    <span class="text-muted text-sm">远程连接管理</span>
    <nav class="ml-6 flex gap-1">
      <a href="/" class="rounded-lg px-3 py-1.5 text-sm transition-colors hover:bg-surface-overlay {{ 'bg-surface-overlay text-white' if active_page == 'dashboard' else 'text-muted' }}">连接列表</a>
      <a href="/audit" class="rounded-lg px-3 py-1.5 text-sm transition-colors hover:bg-surface-overlay {{ 'bg-surface-overlay text-white' if active_page == 'audit' else 'text-muted' }}">审计日志</a>
    </nav>
    <span class="ml-auto text-xs text-muted hidden sm:inline">v{{ version }}</span>
  </div>
</header>
